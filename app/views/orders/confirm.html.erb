<h1>購入確認</h1>

<div>
  <p>商品名: <%= @order.name %></p>
  <p>価格: <%= number_to_currency(@order.price, unit: "¥") %></p>
</div>

<div style="margin-top: 20px;">
  <%= link_to '購入を確定する', orders_path(@order), method: :patch, class: 'btn btn-success' %>
  <%= link_to 'キャンセル', order_path(@order), class: 'btn btn-secondary' %>
</div>
